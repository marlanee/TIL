# 과제 하며 몰랐던 것들
```python
many_zero_list = [0] * 250000
#list 변수에 0이라는 정수를 25만개 할당함
#괄호 안에 문자를 집어넣고 닫은 후 연산식을 작성하면 되는군.
```
### 딕셔너리 활용하기
```python
data = {'과목': 'Python', '구분': '실습', '단계': 2, '문제번호': 3251, '이름': None, '일차': 22}
print(data)
'''
data['단계'] = (f'{'단계'}단계') # 문자열로 형번환 1트. 맞는지는 모르겠음
print(data)['단계']

data['단계'] = str(data['단계'])단계
print(data['단계']) 
# 2트

data['단계'] = str(data)['단계']단계
print(data['단계'])
#3트 실패
'''

data['단계'] = f"{(data['단계'])}단계"

title = '딕셔너리 활용하기'
data['이름'] = title

data['일차'] -= 20
print(data)
```