# 강사님 실습
0. 개인용 SSAFY GMS를 발급받았다.
1. API 키를 왜 코드에 쓰면 안되는가?
2. Temperature을 못 쓰는 모델은 무엇으로 조절하는가?
3. 데이터를 만든 LLM은 
### Chatgpt 호출하기
1. openai 라이브러리와 클래스를 호출한다
2. openai 클래스에 GMS 주소와 API 키를 넣으면 LLM과 통신이 가능해진다
3. `client.responses.create` 메서드로 API로 요청을 보낼 수 있다.
4. API를 코드에 쓰면 탈탈 털리게 된다
5. Temperature을 못 쓴다면, 프롬프트를 통해 조절하면 된다.
6. JSON 응답 형식 정의
7. JSON 형식으로 프롬프트를 작성해서 LLM에 요구한다.
